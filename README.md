Routing in Hono applications

# Standerd http methods : uses all the methods seperately
  
  1. Declaring the routes independently in a file and then using then independently
  2. Encapsulating the routes in a function (single) most commonly used
  3. Using different functions to encapsulate different routes ( used for different features )
  4. Using sub routers like for routing and maintaining routes in different folders